import SmartPasteCore

/// The `Hotkey` adapter that will register the global smart-paste shortcut.
///
/// Scaffold only: no shortcut is registered yet.
public struct GlobalHotkey: Hotkey {
    public init() {}
}
